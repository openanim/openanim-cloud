# Hoist only the packages Next.js and ESLint need from root node_modules.
# Do NOT use shamefully-hoist=true — it breaks workerd's binary resolution.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
public-hoist-pattern[]=@next/swc-*
